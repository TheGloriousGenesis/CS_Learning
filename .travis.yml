language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t exampleNode -f Docker/ExampleNode/Dockerfile.dev .

script:
  - docker run -e CI=true exampleNode npm run test